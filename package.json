{
  "scripts": {
    "dev": "nodemon --config ./server/nodemon.json",
    "build": "npm run build:client && npm run build:server",
    "build:client": "tsc -p ./client",
    "build:server": "tsc -p ./server",
    "postinstall": "npm run build",
    "lint": "eslint . --ext .ts",
    "prestart": "npm run build",
    "start": "ts-node ./dist/server/app.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/helmet": "^4.0.0",
    "@types/node": "^20.10.2",
    "eslint": "^8.55.0",
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "helmet": "^7.1.0"
  }
}
